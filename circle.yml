## Customize the test machine
machine:
    php:
        version:
            5.5.9
    environment:
        CIRCLE_ENV: test

dependencies:
    pre:

checkout:
    post:
        - git submodule sync
        - git submodule update --init

test:
    override:
        - phpunit tests # use PHPunit for testing
    post:
