## Customize the test machine
machine:
    php:
        version:
            5.4.5
    environment:

dependencies:
    pre:

checkout:
    post:
        - git submodule sync
        - git submodule update --init

test:
    post:
