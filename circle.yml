## Customize the test machine
machine:
    php:
        version:
            5.6
    environment:

dependencies:
    pre:

checkout:
    post:
        - git submodule sync
        - git submodule update --init

test:
    post:
